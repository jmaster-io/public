#huong dan chay docker-compose
docker-compose up -d --build

#DOI localhost trong application.properties -> ${HOST_IP_ADDRESS} . cau hinh env  HOST_IP_ADDRESS ve ip_host dang chay
doi localhost cua broker vd: PLAINTEXT://localhost:9092  -> ip_host:9092